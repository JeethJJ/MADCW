<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="github.jeethjj.paw">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        android:hardwareAccelerated="false">
        <activity android:name=".GameGrade"

            android:parentActivityName=".Home" />
        <activity
            android:name=".WrongChoiceImage"
            android:theme="@style/AppTheme.popup" />
        <activity
            android:name=".SlideShow"
            android:parentActivityName=".Home" />
        <activity
            android:name=".IdentifyDog"
            android:parentActivityName=".Home" />
        <activity
            android:name=".WrongChoice"
            android:theme="@style/AppTheme.popup" />
        <activity
            android:name=".CorrectChoice"
            android:theme="@style/AppTheme.popup" />
        <activity
            android:name=".IdentifyBreed"
            android:parentActivityName=".Home" /> <!-- android:configChanges="orientation|screenSize|keyboardHidden" -->
        <activity android:name=".Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>